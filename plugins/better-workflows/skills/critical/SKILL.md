---
name: critical
description: Goal-first 關鍵模式；不可逆 side effects 與完整 fail-closed gates。選擇 $better-workflows:critical 時使用。
---

# Critical

Read `../better-workflows/SKILL.md` completely and follow it, including the Goal-first entry contract.

Select the template dynamically and fix mode to `critical`. Require all policy-mandated independent evidence and external reviewers; missing mandatory evidence ends `inconclusive`, never success.
