---
name: browser-qa
description: Goal-first Webwright／模擬器 QA、截圖與 action log。選擇 $better-workflows:browser-qa 時使用。
---

# Browser and simulator QA

Read `../better-workflows/SKILL.md` completely and follow it, including the Goal-first entry contract.

Use template `browser-simulator-qa` and select the mode dynamically. Delegate browser execution to the applicable domain skill, preferring Webwright when repository policy requires it, and verify current UI state rather than stale snapshots.
