---
name: cross-platform
description: Goal-first Backend、iOS、Android、Web 合約檢查與實作。選擇 $better-workflows:cross-platform 時使用。
---

# Cross-platform contract

Read `../better-workflows/SKILL.md` completely and follow it, including the Goal-first entry contract.

Use template `cross-platform-contract` and select the mode dynamically. Verify server responses, client models, optionality, enums, version gates, and required contract headers across every affected platform.
