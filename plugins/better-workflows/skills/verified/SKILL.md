---
name: verified
description: Goal-first 驗證模式；加入 1–3 個唯讀 agents 與 freshness evidence。選擇 $better-workflows:verified 時使用。
---

# Verified

Read `../better-workflows/SKILL.md` completely and follow it, including the Goal-first entry contract.

Select the template dynamically and use mode `verified` as the minimum. Raise risk when evidence requires it; never lower the selected mode.
