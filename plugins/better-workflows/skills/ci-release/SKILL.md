---
name: ci-release
description: Goal-first CI、runner、deploy、release 監控與修復。選擇 $better-workflows:ci-release 時使用。
---

# CI and release

Read `../better-workflows/SKILL.md` completely and follow it, including the Goal-first entry contract.

Use template `ci-release-monitor` and select the mode dynamically. Refresh current remote state, serialize constrained deployments, and require receipts for authorized side effects.
