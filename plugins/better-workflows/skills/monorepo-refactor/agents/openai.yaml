interface:
  display_name: "monorepo-refactor"
  short_description: "Refactor monorepos with guarded evidence gates"
  default_prompt: "Use $monorepo-refactor to inventory the monorepo, implement every eligible refactor recommendation, and validate each slice with goal checkpoints and rollback evidence."
